#include "main.h"
#include <stdio.h>

/**
 * main - check the code is upper.
 *
 * Return: Always 0.
 */

int _isupper(void)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
